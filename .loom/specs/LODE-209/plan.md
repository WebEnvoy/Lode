# Plan

## Steps

1. Read repository rules, Lode docs, #199/#209/#210/#211/#212, milestone #13 story pointers, #197 ADR, and read-only BOSS research locators.
2. Add BOSS job-search and read-job-detail package assets using the existing package validator contract.
3. Add resource readiness and failure classification for login, identity, page readiness, CAPTCHA, securityId refs, job expiry, permissions, fields, and runtime resources.
4. Register packages in the repo-local registry and query fixture.
5. Add a contract document explaining source absorption and non-absorbed runtime/write behavior.
6. Validate package assets, registry, Python tooling, whitespace, Loom fact chain, suite carriers, and PR metadata/head binding.
