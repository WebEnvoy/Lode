# Plan

## Steps

1. Add the minimal repo-local validate-only package fixture and registry entry.
2. Extend the validator only where existing read-only assumptions block validate-only/draft/preview package validation.
3. Run package validation, sensitive material scan through validator, and diff checks.
