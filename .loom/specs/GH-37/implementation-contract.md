# Implementation Contract

- Write scope: `docs/adr/0002-capability-package-minimum-format.md` and `.loom/**/GH-37*` carrier files, plus `.loom/status/current.md` only for active item readout.
- Forbidden scope: real capability package, schema, fixture, validator, registry, runtime code, other repositories, issue closeout, merge, and `INIT-0001`.
- Validation floor: `git diff --check`, available low-cost repo checks, Loom local checks with blocker classification, and hosted checks after PR creation if available.
