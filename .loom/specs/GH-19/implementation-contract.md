# Implementation Contract

- Write scope: `docs/adr/pending-decisions.md` and `.loom/**/GH-19*` carrier files.
- Forbidden scope: full specs, schema/API/runtime code, package scaffolding, real capability package implementation, fixtures, generated artifacts, marketplace, crawler queue, benchmark contract, external writes, and issue closeout.
- Validation floor: whitespace diff check, local Loom diagnostics with failure classification, and hosted required checks after PR creation.
